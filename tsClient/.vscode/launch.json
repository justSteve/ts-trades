{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run with Poetry",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/main.py",
            "console": "integratedTerminal",
            "python": "${command:python.interpreterPath}",
            "cwd": "${workspaceFolder}",
            "env": {},
            "args": []
        }
    ]
}
